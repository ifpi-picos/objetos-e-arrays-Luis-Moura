import soma from "../calculadora/soma";
import sub from "../calculadora/sub";
import mult from "../calculadora/mult";
import divid from "../calculadora/divid";
import potencia from "../calculadora/potencia";

console.log(`${soma(1,2)}\n${sub(2,3)}\n${mult(4,5)}\n${divid(5,5)}\n${potencia(10,2)}`)